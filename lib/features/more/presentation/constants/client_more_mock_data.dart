/// Placeholder profile data the settings header is previewed against until
/// the screen reads the signed-in client from `SessionCubit`.
///
/// Sample values, not user-facing copy: they stand in for what the backend
/// will supply, which is why they are deliberately not translated — the same
/// reasoning as `OrderMockData`.
abstract final class ClientMoreMockData {
  static const String clientName = 'محمد أحمد';
  static const String stationCount = '2';
  static const String clientCode = 'GS-MA-526';
}
