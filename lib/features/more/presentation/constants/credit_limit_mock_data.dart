/// Placeholder values the credit-limit screen is previewed against until it
/// is wired to the API.
///
/// Sample data, not user-facing copy — the same reasoning as `OrderMockData`,
/// which is why these are deliberately not translated.
abstract final class CreditLimitMockData {
  /// When the submitted request was filed. Fixed while the screen has no
  /// backend to ask.
  static const String requestDate = '9 ربيع الأول 1448';
}
