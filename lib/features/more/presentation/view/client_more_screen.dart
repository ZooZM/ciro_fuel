import 'package:flutter/material.dart';

class ClientMoreScreen extends StatelessWidget {
  const ClientMoreScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('المزيد'));
  }
}
